#include<bits/stdc++.h>
using namespace std;
int d, x, y;
int main()
{
    unordered_set<int>s;
    unordered_set<int>:: iterator it;
    /*s.insert(4);
    s.insert(1);
    s.insert(5);
    s.insert(3);
    s.insert(5);
    s.erase(s.begin());
    s.erase(s.begin());
    s.erase(s.begin());*/
    cout<<s.size();
    for(it=s.begin();it!=s.end();it++)
        cout<<" "<<*it;


}
