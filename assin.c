#include <stdio.h>
int main()
{
    int i,t;
    scanf("%d",&t);
    for(i=1;i<=t;i++){
        char a[200];
        int b,len;
        scanf("%s %d",a,&b);
        len=alen;
        int rem=0
        for(j=1;j<=len;j++){
        if(a%b!=0){
            rem=10*rem+a[len];

        }
        }
        printf("%d",rem)
    }
}
