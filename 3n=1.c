#include<stdio.h>
int main()
{
    char a[100],;
    int i,cnt=0;
    gets(a);
    for(i=0;a[i]!='\0';i++)
    {
        cnt++;
        for(j=9;j<=0;j--)
        {
            b[j]=a[i];
        }
    }
    printf("%d\n",cnt);
    puts(b);
}
