#include<stdio.h>
#include<algorithm>
#include<vector>
using namespace std;
long long n,k,a[200000],i,j=0,b;
int main()
{
    vector<long long>v;

    scanf("%lld %lld",&n,&k);
    for(i=0;i<n;i++)
    {
        scanf("%lld",&b);
        v[i+j]=b;
        j=i+1;

    }
    for(i=0;i<n;i++)
    {
       b[i]=
    }
    printf("%lld\n",v2[k-1]);

}
